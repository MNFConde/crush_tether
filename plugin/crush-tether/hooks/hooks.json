{
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "process",
            "command": "${ZCODE_PLUGIN_ROOT}/bin/crush-tether-wrapper.cmd",
            "args": ["hook", "--agent", "zcode"],
            "timeoutMs": 8000
          }
        ]
      }
    ]
  }
}
